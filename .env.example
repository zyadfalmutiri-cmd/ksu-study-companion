# Client-side (safe to expose in the browser — publishable key only)
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Server-only — never prefix with VITE_, never commit real values
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# LLM / embeddings — fill in once a provider is chosen (see docs/PRODUCT_SPEC.md §6)
LLM_API_KEY=
EMBEDDING_API_KEY=
